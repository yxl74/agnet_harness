

<!-- DuoCoder:start -->
## DuoCoder — Peer Agent Context

You are running alongside **Claude Code** on this project. Claude's full terminal output is mirrored to a file you can read.

### Read Claude's output: `.duo/claude-output.txt`

This file contains Claude Code's **complete terminal session** — everything it said, every file it edited, every command it ran, and its reasoning. It updates in real-time (every 300ms). It's a rolling 50KB window of the most recent output.

**Before starting any task**, read `.duo/claude-output.txt` to see what Claude has been doing. This avoids duplicate work and conflicting edits.

**Your terminal output is also mirrored** to `.duo/codex-output.txt` — Claude reads that file to see what you're doing. Everything you say and do is visible to Claude.

### Rules:
- Do NOT modify files in `.duo/`
- Check Claude's output before editing any file it recently touched
- If you disagree with Claude's approach, explain why — Claude will see your reasoning

<!-- DuoCoder:end -->
